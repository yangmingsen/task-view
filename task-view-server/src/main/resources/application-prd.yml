storage:
  client:
    host: 127.0.0.1
    port: 9005
    sysId: A004_PRD
    token: c6538cf45d7947299a2191522307b517

cors:
  allowed-host-name: api.taskview.yms.top
  allowed-hosts: 127.0.0.1,localhost
  allowed-ports: 9009
  allowed-method: OPTIONS,HEAD,GET,POST,PUT,DELETE,PATCH

server:
  port: 9008

spring:
  datasource:
    username: root
    password: ymsyms
    url: jdbc:mysql://localhost:3306/task_view_prov?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
    type: com.alibaba.druid.pool.DruidDataSource
    initialSize: 5
    minIdle: 1
    maxActive: 50
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000

